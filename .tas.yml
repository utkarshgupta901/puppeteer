framework: mocha

postMerge:
  pattern:
    - "test/*.spec.ts"
preMerge:
  pattern:
    - "test/*.spec.ts"
preRun:
     command:
        - npm install
        - npm run prepare
        - npm run tsc

configFile: "./mocha-config/puppeteer-unit-tests.js"
version: 2.0
